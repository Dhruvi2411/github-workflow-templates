name: just for test

on:
  workflow_call:
    inputs:
      artifact-name:
        required: true
        type: string

      version:
        required: true
        type: string
        
jobs:
    upload:
        runs-on: self-hosted